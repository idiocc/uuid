<types namespace="_goa">
  <type name="UuidConfig" desc="Optional uuid state to apply.">
    <prop opt type="!Array<number>" name="random">
      Array of 16 numbers (0-255) to use in place of randomly generated values.
    </prop>
    <prop opt type="function(): !Array<number>" name="rng">
      Random # generator function that returns an Array[16] of byte values (0-255).
    </prop>
  </type>
</types>